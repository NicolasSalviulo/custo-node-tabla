# Notas;

Este es mi primer programana en Node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Muestra la tabla en consola                           [boolean]
  -h, --hasta    Hasta que número se multiplicará la base [number] [default: 10]
  -d, --desde    Desde que número se multiplicará la base  [number] [default: 1]
```